server:
  port: 8080
  servlet:
    encoding:
      enable: true
      force: true
      charset: UTF-8
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/myboard?serverTimezone=Asia/Seoul&useSSL=false&characterEncoding=utf-8&allowPublicKeyRetrieval=true
    username: developer
    password: dev1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        dialect: org.hibernate.dialect.MySQLDialect